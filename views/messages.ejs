<% if(locals.messages) { %>
    <% locals.messages.forEach((message) => { %>
        <p class="<%= message.type %> ">
            <%= message.string %>
        </p>
        <% }) %>
            <% removeMessages() %>
                <% } %>